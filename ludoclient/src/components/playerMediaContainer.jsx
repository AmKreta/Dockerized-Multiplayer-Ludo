import React from "react";
import { styled } from "@mui/material/styles";

const PlayerVideoContainer = () => {
  return <div>player media</div>;
};

const Container = styled("div")((props) => ({}));
export default PlayerVideoContainer;
